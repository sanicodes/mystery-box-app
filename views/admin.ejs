<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-p+1VpvA8V3mDJbB2V+1mZ7FCljgKDLb2KpV+mUytBy91gEV+uWqlXDj4dD3M3U5r+vKMt1fE5j4u1vYSf1+7eg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>Admin - Mystery Box Results</title>
</head>
<body>
    <header>
        <h1><i class="fas fa-user-shield"></i> Admin Panel - Mystery Box Results</h1>
        <p><a href="/admin-logout"><i class="fas fa-sign-out-alt"></i> Logout</a></p>
    </header>

    <main>
        <table>
            <thead>
                <tr>
                    <th><i class="fas fa-user"></i> Username</th>
                    <th><i class="fas fa-gift"></i> Result</th>
                    <th><i class="fas fa-tools"></i> Action</th>
                </tr>
            </thead>
            <tbody>
                <% if (data.length === 0) { %>
                    <tr>
                        <td colspan="3">No entries found.</td>
                    </tr>
                <% } else { %>
                    <% data.forEach((entry) => { %>
                        <tr>
                            <td><%= entry.username %></td>
                            <td>
                                <% if (entry.outcome === 'Bomb') { %>
                                    <i class="fas fa-bomb" style="color: #dc3545;"></i> <%= entry.outcome %>
                                <% } else { %>
                                    <i class="fas fa-arrow-up" style="color: #28a745;"></i> <%= entry.outcome %>
                                <% } %>
                            </td>
                            <td>
                                <form action="/admin/delete/<%= entry.username %>" method="POST" style="display:inline;">
                                    <button type="submit" class="delete-btn" onclick="return confirm('Are you sure you want to delete this entry?');">
                                        <i class="fas fa-trash-alt"></i> Delete
                                    </button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                <% } %>
            </tbody>
        </table>
    </main>
</body>
</html>
